# [改善] backend ディレクトリの初期実装と品質評価基盤の整備

## 背景
週次品質レポート（2026-04-20）で `backend/` ディレクトリが存在せず、6観点の評価が低スコアとなった。

## 対応内容
- `backend/` の最小構成（Go module、service/repository 層、設定）を追加する。
- lint/test を CI 実行可能にする。
- セキュリティ・運用の基本要件（入力検証、依存監査、ログ/メトリクス）を定義・実装する。

## 完了条件
- `backend/` 配下に実装と `*_test.go` が存在する。
- backend 向け lint/test が CI で実行される。
- 次回スコアリングで6観点すべて実コード根拠で評価できる。
