# [改善] customer-mobile 品質評価不能状態の解消

## 背景

週次品質レポート（2026-05-11）で `customer-mobile/` ディレクトリ実体が確認できず、主要評価観点が5点未満となった。

## 対応内容

- `customer-mobile/` のソースコードと実行に必要な設定ファイルを配置する
- lint / test / type-check をCIに追加し、週次レポートの定量根拠を作る
- セキュリティ（依存関係監査）とパフォーマンス（bundle size等）を継続計測する

## 完了条件

- `customer-mobile/` がリポジトリ上で参照可能である
- CIで `customer-mobile` 向け品質チェックが成功する
- 次回週次品質レポートで全観点の再評価が可能になる
