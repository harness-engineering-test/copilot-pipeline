# 週次品質レポート: backend/ — 2026-04-27

## 対象

`backend/` ディレクトリ（Go）

---

## スコア評価（各10点満点）

> **前提**: `backend/` ディレクトリは現時点でリポジトリに存在しない。  
> 各スコアは「実装ゼロ」の状態を基準として算出する。

| 観点 | スコア | 根拠 |
|------|--------|------|
| 1. アーキテクチャ | 1 / 10 | ディレクトリ自体が存在せず、レイヤー設計・DI・モジュール分割のいずれも実装されていない。`copilot-instructions.md` にはサービス層・リポジトリ層・リゾルバ層の分離方針が定義されており、方針は存在するが実体がない。 |
| 2. コード品質 | 1 / 10 | 評価対象のコードがなく、リンター・フォーマッター設定も未整備。Go プロジェクトとしての `go.mod` すら存在しない。 |
| 3. テスト | 1 / 10 | テストファイル（`*_test.go`）が一切存在しない。CI 上でテストを実行するステップも設定されていない。 |
| 4. セキュリティ | 1 / 10 | 認証・認可の実装なし。シークレット管理・依存ライブラリの脆弱性スキャンも未実施。ただし GitHub Actions ワークフローで `COPILOT_ASSIGN_TOKEN` を secrets 経由で扱っており、CI 側の基本姿勢は適切。 |
| 5. パフォーマンス | 1 / 10 | 評価対象のコードが存在せず、ベンチマーク・プロファイリング設定もない。 |
| 6. 運用性 | 3 / 10 | `backend/` 固有の CI ワークフロー（ビルド・テスト・lint）は未整備だが、リポジトリ全体として GitHub Actions による自動化基盤（コードスコアリング・PR 自動作成等）は整っている。`copilot-instructions.md` に規約が明文化されている点は加点要素。 |

---

## 総合スコア

| 項目 | 値 |
|------|----|
| 合計点 | 8 / 60 |
| 平均点 | 1.3 / 10 |
| **総合ランク** | **D** |

> **判定基準**  
> S: 55–60 / A: 45–54 / B: 35–44 / C: 25–34 / D: 0–24

---

## 改善提案（優先度順）

### 優先度 1: backend/ の基本構造を実装する

`backend/` ディレクトリが存在しないため、すべての評価が最低値になっている。以下を実装することで複数観点のスコアが同時に改善される。

- `go.mod` / `go.sum` の作成（モジュール名決定）
- ディレクトリ構成の確立（`cmd/`, `internal/`, `utils/`, `graph/`, `repository/`, `service/`）
- `golangci-lint` 設定ファイル（`.golangci.yml`）の追加
- GitHub Actions での `go build` / `go test` ステップ追加

**対象観点**: アーキテクチャ・コード品質・運用性

---

### 優先度 2: テスト基盤を整備する

コードが実装された後、テスト戦略を早期に確立することで品質担保コストを最小化できる。

- `_test.go` による単体テストの作成（サービス層・リポジトリ層）
- `testify` または標準 `testing` パッケージの採用方針決定
- GitHub Actions でのカバレッジレポート出力
- モック生成ツール（`mockgen` 等）の導入

**対象観点**: テスト

---

### 優先度 3: セキュリティ対策を組み込む

実装開始と同時にセキュリティ要件を組み込むことで、後からのリファクタリングコストを抑制できる。

- 認証・認可の設計（JWT / セッション管理）
- `govulncheck` / `nancy` による依存ライブラリの脆弱性スキャンを CI に追加
- シークレットの環境変数管理（ハードコード禁止のリンタールール設定）
- SQL インジェクション対策（ent ORM 使用により一定程度担保されるが方針を明文化）

**対象観点**: セキュリティ

---

## 5点未満の観点（改善 Issue 起票対象）

以下の観点はすべて5点未満であり、別途改善 Issue の起票が必要。

| 観点 | スコア | Issue タイトル（案） |
|------|--------|----------------------|
| アーキテクチャ | 1 | `[改善] backend/ の基本ディレクトリ構成とアーキテクチャを実装する` |
| コード品質 | 1 | `[改善] backend/ に golangci-lint 設定と Go モジュールを導入する` |
| テスト | 1 | `[改善] backend/ のテスト基盤（単体テスト・CI 連携）を整備する` |
| セキュリティ | 1 | `[改善] backend/ にセキュリティ対策（認証・脆弱性スキャン）を組み込む` |
| パフォーマンス | 1 | `[改善] backend/ にパフォーマンス計測・ベンチマーク基盤を整備する` |

---

*このレポートは `codebase-scoring` ワークフローにより自動起票された Issue に基づき、Copilot が自動生成しました。*
