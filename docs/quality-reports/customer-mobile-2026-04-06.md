# 週次品質レポート: customer-mobile

**対象**: `customer-mobile/` ディレクトリ  
**評価日**: 2026-04-06  
**評価者**: Copilot

---

## スコアサマリー

| 観点 | スコア | 判定 |
|------|--------|------|
| アーキテクチャ | 1 / 10 | ❌ 要改善 |
| コード品質 | 1 / 10 | ❌ 要改善 |
| テスト | 0 / 10 | ❌ 要改善 |
| セキュリティ | 1 / 10 | ❌ 要改善 |
| パフォーマンス | 1 / 10 | ❌ 要改善 |
| 運用性 | 1 / 10 | ❌ 要改善 |
| **合計** | **5 / 60** | |

**総合ランク: D**

---

## 各観点の評価

### 1. アーキテクチャ（1 / 10）

**現状**:
- `customer-mobile/` ディレクトリが存在しない。プロジェクトとしての構造が未定義。
- `.github/copilot-instructions.md` に「React Native/Expo」による実装が想定されていることは記載されている。
- `.github/instructions/mobile.instructions.md` は空のプレースホルダーのみ。

**根拠**:
ディレクトリが存在しないため、アーキテクチャを評価するコードベースそのものが未実装。プロジェクト概要には技術スタックの記載があるため、最低評価の1点とする。

---

### 2. コード品質（1 / 10）

**現状**:
- ソースコードが存在しない。
- ESLint・Prettier 等のコード品質ツールの設定ファイルが未作成。
- TypeScript の型定義、コンポーネント設計などが評価不能。

**根拠**:
実装がないためコード品質を測定できない。

---

### 3. テスト（0 / 10）

**現状**:
- テストファイルが存在しない（`*.test.ts` / `*.spec.ts`）。
- テスト実行環境（Jest・Testing Library 等）が未構築。

**根拠**:
テストコードが一切存在しないため 0 点とする。

---

### 4. セキュリティ（1 / 10）

**現状**:
- 実装が存在しないため、認証・認可・データ保護等のセキュリティ要件が未定義。
- シークレット管理（環境変数・キーストア）の方針が未記載。

**根拠**:
リポジトリのプロジェクト概要にモバイルアプリとして想定があることから 1 点。実装がないため評価不能。

---

### 5. パフォーマンス（1 / 10）

**現状**:
- バンドルサイズ、描画パフォーマンス、API レスポンスハンドリングなどを評価するコードが存在しない。
- パフォーマンス計測・監視の設定が未定義。

**根拠**:
実装がないため評価不能。1 点は計画段階としての最低評価。

---

### 6. 運用性（1 / 10）

**現状**:
- CI/CD パイプラインに `customer-mobile` 向けの設定が存在しない。
- エラー監視（Sentry 等）、ログ収集、クラッシュレポートが未設定。
- App Store / Google Play 向けのビルド・配布自動化が未設定。

**根拠**:
実装がないため運用性を評価できない。

---

## 総合ランク

**D ランク**

すべての評価観点で 5 点未満であるため、最低ランクと判定する。
`customer-mobile/` ディレクトリ自体が存在しないため、以下の改善 Issue を起票する。

---

## 改善提案（優先度順）

### 優先度 1: customer-mobile プロジェクトの初期構築 ⭐⭐⭐

React Native / Expo を使用した `customer-mobile/` プロジェクトのスキャフォールディングを実施する。

- `npx create-expo-app customer-mobile --template` による初期化
- TypeScript 設定（`tsconfig.json`）
- ESLint / Prettier の導入と設定
- `package.json` への必要な依存関係の追加

**対応 Issue**: #[要起票] `[改善] customer-mobile プロジェクトの初期構築`

---

### 優先度 2: テスト基盤の構築 ⭐⭐

Jest + React Native Testing Library を使用したテスト環境を構築し、主要コンポーネント・フック・ユーティリティの単体テストを実装する。

- `jest.config.ts` の設定
- `@testing-library/react-native` の導入
- CI でのテスト自動実行設定（GitHub Actions）

**対応 Issue**: #[要起票] `[改善] customer-mobile テスト基盤の構築`

---

### 優先度 3: セキュリティ・運用基盤の整備 ⭐⭐

アプリのセキュリティと運用監視の基盤を整備する。

- 環境変数管理（`react-native-dotenv` または Expo の `app.config.js`）
- クラッシュレポート（Sentry for React Native）の導入
- CI/CD パイプラインへの `customer-mobile` ビルド・テストジョブの追加

**対応 Issue**: #[要起票] `[改善] customer-mobile セキュリティ・運用基盤の整備`

---

## 起票が必要な改善 Issue 一覧

すべての観点で 5 点未満のため、以下の改善 Issue を別途起票する。

| Issue タイトル | 観点 | 優先度 |
|---------------|------|--------|
| `[改善] customer-mobile プロジェクトの初期構築` | アーキテクチャ / コード品質 | 高 |
| `[改善] customer-mobile テスト基盤の構築` | テスト | 高 |
| `[改善] customer-mobile セキュリティ・運用基盤の整備` | セキュリティ / 運用性 | 中 |
