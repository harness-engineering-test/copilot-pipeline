# 週次品質レポート: customer-mobile

**対象ディレクトリ**: `customer-mobile/`
**レポート日**: 2026-06-01
**評価者**: Copilot Cloud Agent

---

## 評価サマリー

| 観点 | スコア（/10） | ランク |
|------|-------------|--------|
| アーキテクチャ | 1 | D |
| コード品質 | 1 | D |
| テスト | 1 | D |
| セキュリティ | 1 | D |
| パフォーマンス | 1 | D |
| 運用性 | 1 | D |
| **合計** | **6 / 60** | **D** |

**総合ランク: D**

---

## 評価詳細

### 1. アーキテクチャ（1/10）

**根拠**:
`customer-mobile/` ディレクトリが存在しない。React Native/Expo アーキテクチャとして想定されているが、実装ファイルが一切ない状態のため、アーキテクチャ評価が不可能。`copilot-instructions.md` では「モノレポ構成。customer-mobile/ (React Native/Expo)」と定義されているが、対応する実装が存在しない。

スコア: **1点**（ディレクトリ定義のみ存在）

---

### 2. コード品質（1/10）

**根拠**:
評価対象のソースコードが存在しない。`mobile.instructions.md` には「後続フェーズで詳細を記載」とのみ記載されており、コーディング規約・実装ガイドラインも未整備。Linter 設定、型定義、コンポーネント設計方針も未定義。

スコア: **1点**（構造定義のみ、実装なし）

---

### 3. テスト（1/10）

**根拠**:
テストファイルが存在しない（`*.test.ts` / `*.spec.ts` の形式を期待）。`copilot-instructions.md` には「新規コードには必ず単体テストを書く」と定めているが、テスト対象のコード自体がない。Jest / Testing Library などのテスト環境も未構築。

スコア: **1点**（テスト方針の定義なし、ファイルなし）

---

### 4. セキュリティ（1/10）

**根拠**:
認証処理・API通信・データ保存に関するセキュリティ実装が存在しない。依存パッケージ管理（`package.json`）もなく、脆弱性スキャン対象がない状態。セキュリティ要件・脅威モデルも未定義。

スコア: **1点**（評価不能、実装なし）

---

### 5. パフォーマンス（1/10）

**根拠**:
パフォーマンス計測・最適化の対象となるコンポーネントが存在しない。バンドルサイズ、レンダリング最適化、API レスポンスキャッシュ等の観点は実装なし。Expo のビルド設定も未存在。

スコア: **1点**（評価不能、実装なし）

---

### 6. 運用性（1/10）

**根拠**:
エラー監視（Sentry 等）、ログ出力、クラッシュレポート設定が未実装。CI/CD パイプラインの `preview-deploy.yml` は存在するが、`customer-mobile/` 向けの具体的なビルド・デプロイ手順がない。環境変数管理・リリース手順も未整備。

スコア: **1点**（CI workflow は存在するが customer-mobile 固有の運用設定なし）

---

## 総合ランク判定

**D ランク**（合計 6/60点、10%）

全観点において `customer-mobile/` の実装が存在しないため、最低評価となった。これはコード品質の問題ではなく、実装フェーズ未着手であることを示す。

---

## 改善提案（優先度順）

### 優先度1: customer-mobile/ の初期実装立ち上げ（最高優先）

React Native / Expo プロジェクトの初期構築を行う。

```bash
npx create-expo-app customer-mobile --template
```

- フォルダ構造の定義（`src/screens/`, `src/components/`, `src/hooks/`, `src/api/` 等）
- ESLint / Prettier 設定
- TypeScript strict mode の有効化
- `mobile.instructions.md` に具体的なコーディング規約を記載

**期待効果**: アーキテクチャ・コード品質スコア → 5〜7点

---

### 優先度2: テスト基盤の構築

Jest + React Native Testing Library の導入と最低限のテストカバレッジ確保。

- `jest.config.ts` の設定
- 主要コンポーネントのスナップショットテスト
- カスタムフックの単体テスト
- カバレッジ閾値の設定（目標: 60%以上）

**期待効果**: テストスコア → 5〜7点

---

### 優先度3: セキュリティ・運用基盤の整備

- `expo-secure-store` を用いたトークン管理
- API 通信の HTTPS 強制・証明書ピンニング
- Sentry SDK の組み込みとクラッシュレポート設定
- `.env` / `app.config.ts` による環境変数管理
- CI での `npm audit` / `expo doctor` 自動実行

**期待効果**: セキュリティ・運用性スコア → 5〜7点

---

## 改善 Issue 起票リスト

全観点のスコアが 5点未満（1点）のため、以下の改善 Issue を起票する。

| Issue タイトル | 観点 | 優先度 |
|--------------|------|-------|
| `[改善] customer-mobile 初期プロジェクト構築（アーキテクチャ・コード品質）` | アーキテクチャ、コード品質 | 最高 |
| `[改善] customer-mobile テスト基盤の構築` | テスト | 高 |
| `[改善] customer-mobile セキュリティ・運用基盤の整備` | セキュリティ、パフォーマンス、運用性 | 高 |

---

## 備考

本レポートは `customer-mobile/` ディレクトリが存在しない状態での評価である。次週のレポートでは実装着手後の評価を行うことを想定する。
