# 週次品質レポート — backend — 2026-05-25

## 対象

`backend/` ディレクトリ

---

## スコア一覧

| 観点 | スコア | ランク |
|------|--------|--------|
| アーキテクチャ | 1 / 10 | D |
| コード品質 | 1 / 10 | D |
| テスト | 1 / 10 | D |
| セキュリティ | 1 / 10 | D |
| パフォーマンス | 1 / 10 | D |
| 運用性 | 2 / 10 | D |
| **合計** | **7 / 60** | **D** |

---

## 各観点の根拠

### 1. アーキテクチャ（1 / 10）

`backend/` ディレクトリが存在しない。  
`copilot-instructions.md` には Go + ent によるモノレポ構成（service 層・repository 層・GraphQL resolver・convert/ パッケージ）が定義されているが、実装ファイルが一切ない。  
設計規約だけが存在し、それを実現するコードがないため最低点とした。

### 2. コード品質（1 / 10）

評価対象のソースコードが存在しないため最低点。  
`copilot-instructions.md` にコーディング規約（同一レイヤー呼び出し禁止・utils 優先・ent の直接 import 禁止）が定義されており、規約自体は整備されているが、遵守状況を確認できない。

### 3. テスト（1 / 10）

`*_test.go` ファイルが存在しない。  
テスト方針（新規コードには必ず単体テストを書く）は `copilot-instructions.md` に記載されているが、テストコードがゼロのため最低点。

### 4. セキュリティ（1 / 10）

実装コードが存在しないため、認証・認可・入力バリデーション・シークレット管理のいずれも評価不能。  
ただし `.github/` 配下の secrets 管理（`COPILOT_ASSIGN_TOKEN`）は適切にワークフロー変数として扱われており、それだけを評価して 1 点とした。

### 5. パフォーマンス（1 / 10）

評価対象のコードが存在しないため最低点。  
DB クエリ設計・キャッシュ・N+1 対策などを検証できない。

### 6. 運用性（2 / 10）

`backend/` 自体は存在しないが、以下の CI/CD 整備が確認できたため 2 点とした。

- `codebase-scoring.yml`: 週次品質レポートの自動発行
- `check-migration.yml`: マイグレーション安全性チェック
- `detect-unused-code.yml`: 未使用コード検出
- `pipeline-stuck-monitor.yml`: パイプライン監視

アプリケーションのヘルスチェック・ロギング・メトリクス収集は未実装。

---

## 総合ランク

**D**（実装が存在しないため全観点で最低評価）

---

## 改善提案（優先度順）

### 優先度 1 — backend ディレクトリの初期実装【緊急】

**背景**: 全スコアの根本原因はコードが存在しないことにある。  
**アクション**:
1. `backend/` を Go モジュールとして初期化（`go mod init`）
2. `copilot-instructions.md` の規約に従いレイヤー構造を作成  
   ```
   backend/
   ├── cmd/              # エントリポイント
   ├── graph/            # GraphQL スキーマ・resolver
   │   └── convert/      # 型変換
   ├── service/          # ビジネスロジック
   ├── repository/       # DB アクセス抽象化
   ├── ent/              # ent スキーマ
   └── utils/            # ptr.go 等の共有ユーティリティ
   ```
3. 各レイヤーに基本的な CRUD + ユニットテストを実装

### 優先度 2 — テスト基盤の整備【高】

**背景**: テストが 0 件。CI でカバレッジレポートも取得できない。  
**アクション**:
1. `go test ./...` + カバレッジ計測を CI に組み込む
2. 各サービス・リポジトリにテーブル駆動テストを実装
3. `testing.instructions.md` に具体的なテスト規約を追記

### 優先度 3 — セキュリティ基盤の実装【高】

**背景**: 認証・認可・入力バリデーションが未実装。  
**アクション**:
1. JWT / OAuth2 による認証ミドルウェアを実装
2. GraphQL ディレクティブ（`@hasRole` など）で認可を表現
3. 入力値バリデーションライブラリ（例: `go-playground/validator`）を導入
4. `SECURITY.md` を作成し脆弱性報告フローを文書化

---

## 改善 Issue 起票対象（スコア 5 未満の観点）

以下の観点はすべて 5 点未満のため、別途改善 Issue を起票する。

| 観点 | スコア | Issue タイトル案 |
|------|--------|-----------------|
| アーキテクチャ | 1 | `[改善] backend/ ディレクトリの初期アーキテクチャ実装` |
| コード品質 | 1 | `[改善] backend コーディング規約の実装適用` |
| テスト | 1 | `[改善] backend ユニットテスト基盤の整備` |
| セキュリティ | 1 | `[改善] backend 認証・認可・入力バリデーションの実装` |
| パフォーマンス | 1 | `[改善] backend DB クエリ設計とパフォーマンス基盤の整備` |

---

*生成日時: 2026-05-25*  
*次回レポート予定: 2026-06-01（毎週月曜 03:00 UTC 自動実行）*
