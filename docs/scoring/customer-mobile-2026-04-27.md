# 週次品質レポート: customer-mobile

- **対象**: `customer-mobile/` ディレクトリ
- **評価日**: 2026-04-27
- **評価者**: Copilot (自動)

---

## 評価結果サマリー

| 観点 | スコア（/10） |
|------|-------------|
| 1. アーキテクチャ | 0 |
| 2. コード品質 | 0 |
| 3. テスト | 0 |
| 4. セキュリティ | 0 |
| 5. パフォーマンス | 0 |
| 6. 運用性 | 0 |
| **合計** | **0 / 60** |

**総合ランク: D**

---

## 評価詳細

### 1. アーキテクチャ — 0/10

`customer-mobile/` ディレクトリがリポジトリに存在しません。  
React Native/Expo プロジェクトとして設計される予定ですが、未着手の状態です。  
ディレクトリ構造・レイヤー設計・依存関係の整理が一切行われていないため、評価対象なし（0点）とします。

### 2. コード品質 — 0/10

ソースファイルが存在しないため評価不可（0点）。

### 3. テスト — 0/10

テストファイル（`*.test.ts` / `*.spec.ts`）が存在しないため評価不可（0点）。

### 4. セキュリティ — 0/10

実装がないため評価不可（0点）。  
認証・通信・データ保護などのセキュリティ実装を確認できません。

### 5. パフォーマンス — 0/10

実装がないため評価不可（0点）。  
レンダリング最適化・バンドルサイズ・API呼び出しの設計を確認できません。

### 6. 運用性 — 0/10

実装がないため評価不可（0点）。  
ログ・エラーハンドリング・環境変数管理などを確認できません。

---

## 総合ランク判定

**D**（実装未着手のため評価対象なし）

---

## 改善提案（優先度順）

### 優先度1: customer-mobile プロジェクトの初期構築

`customer-mobile/` ディレクトリが存在しないため、まず Expo (React Native) プロジェクトの初期構築を行う必要があります。

- `npx create-expo-app customer-mobile` でプロジェクトを生成
- `src/` 配下に `screens/`・`components/`・`hooks/`・`utils/` のディレクトリ構造を設計
- TypeScript を有効化し、ESLint / Prettier を設定

### 優先度2: テスト基盤の整備

プロジェクト生成と同時にテスト基盤を整備し、0点のテストスコアを改善します。

- Jest + React Native Testing Library を導入
- `*.test.ts` / `*.spec.ts` のテストファイル規約を定義
- CI/CD パイプラインへのテスト実行ステップの追加

### 優先度3: セキュリティ要件の定義と実装

モバイルアプリとして最低限のセキュリティ要件を定義し、実装します。

- 認証トークンの安全な保管（`expo-secure-store` の利用）
- 通信の HTTPS 強制と証明書ピンニング検討
- 機密情報をソースコードにハードコードしないルールの明文化

---

## スコア5点未満の観点（改善 Issue 対象）

全観点が 0/10 のため、以下の改善 Issue 起票を推奨します：

- `[改善] customer-mobile プロジェクトの初期構築`
- `[改善] customer-mobile テスト基盤の整備`
- `[改善] customer-mobile セキュリティ要件の定義と実装`
