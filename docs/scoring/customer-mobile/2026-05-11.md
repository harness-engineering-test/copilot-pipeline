# customer-mobile 週次品質レポート（2026-05-11）

対象: `customer-mobile/`

## 評価結果（各10点満点）

| 観点 | スコア | 根拠 |
| --- | ---: | --- |
| アーキテクチャ | 2 | リポジトリ内に `customer-mobile/` ディレクトリが存在せず、レイヤ構成や責務分離を確認できない。 |
| コード品質 | 1 | 対象コードが存在しないため、静的品質（可読性・保守性・重複・規約準拠）を評価できない。 |
| テスト | 1 | 対象ディレクトリ配下のテストコード・テスト設定・実行結果を確認できない。 |
| セキュリティ | 2 | 対象アプリ実装が存在しないため、入力検証・認証認可・依存関係脆弱性対策を評価できない。 |
| パフォーマンス | 2 | 実装・計測手段が存在せず、描画/通信/バンドルサイズなどの確認ができない。 |
| 運用性 | 3 | 週次スコアリングWorkflowは存在するが、対象プロジェクト実体不在のため運用改善ループを回せない。 |

## 総合ランク

**D**

## 改善提案（優先度順）

1. `customer-mobile/` の実体（ソース、設定、依存関係）をリポジトリに追加し、最低限のビルド/テストコマンドを整備する。
2. CIで `customer-mobile/` 向けのlint/test/type-checkを定義し、週次スコアリングの根拠を自動収集できる状態にする。
3. セキュリティ・パフォーマンス観点の定点チェック（依存関係監査、bundle size、主要画面の計測）をWorkflowに追加する。

## 5点未満観点の改善Issue

5点未満の観点があるため、別途改善Issue案を作成:  
`docs/issues/2026-05-11-customer-mobile-quality-recovery.md`
