# 週次品質レポート: backend/ — 2026-05-11

## サマリー

| 観点 | スコア | 評価 |
|------|--------|------|
| アーキテクチャ | 2 / 10 | D |
| コード品質 | 1 / 10 | D |
| テスト | 1 / 10 | D |
| セキュリティ | 1 / 10 | D |
| パフォーマンス | 1 / 10 | D |
| 運用性 | 3 / 10 | D |
| **合計** | **9 / 60** | **D** |

**総合ランク: D**

---

## 評価詳細

### 1. アーキテクチャ（2 / 10）

**根拠:**
- `backend/` ディレクトリが存在しない。実装ゼロの状態。
- `.github/copilot-instructions.md` にアーキテクチャ方針（レイヤー分離・サービス層・リポジトリ抽象化）は定義されているが、実体となるコードが存在しない。
- `backend.instructions.md` はプレースホルダのみで詳細未記載。

### 2. コード品質（1 / 10）

**根拠:**
- 評価対象コードが存在しない。
- 規約（ポインタ変換は `ptr.go` 経由、同一レイヤー間呼び出し禁止など）は定義されているが適用実績なし。

### 3. テスト（1 / 10）

**根拠:**
- `_test.go` ファイルが存在しない。
- テスト方針（新規コードには必ず単体テスト）は定義されているが、実装がないためテストも存在しない。

### 4. セキュリティ（1 / 10）

**根拠:**
- 実装がないためセキュリティ評価不可。
- 認証・認可・入力バリデーション・シークレット管理の実装がすべて未着手。

### 5. パフォーマンス（1 / 10）

**根拠:**
- 実装がないためパフォーマンス評価不可。
- N+1 対策・クエリ最適化・キャッシュ戦略の実装が未着手。

### 6. 運用性（3 / 10）

**根拠:**
- CI/CD ワークフロー（`codebase-scoring.yml`、`check-migration.yml`、`copilot-fix-ci.yml` など）が整備されており、自動化の基盤は存在する。
- ただし `backend/` のビルド・テスト・デプロイパイプラインがなく、ロギング・モニタリング・ヘルスチェックも未実装。

---

## 総合ランク判定: D

全観点で 5 点未満のため、総合ランクは **D** です。  
（目安: S=55〜60, A=45〜54, B=35〜44, C=25〜34, D=25未満）

---

## 改善提案（優先度順）

### 優先度1: バックエンド基盤の実装

**概要:** `backend/` ディレクトリを新規作成し、プロジェクト方針に沿った基盤を構築する。

- Go モジュール初期化（`go.mod`）
- ディレクトリ構成: `handler/`、`service/`、`repository/`、`utils/`、`ent/`
- `utils/ptr.go` などの共有ユーティリティを先行実装
- GraphQL スキーマ定義と `ggen` によるコード生成

**期待効果:** アーキテクチャ・コード品質スコアが大幅改善（4〜6点水準）

---

### 優先度2: テスト基盤の整備

**概要:** バックエンド実装と並行して、単体テスト基盤を整備する。

- テストヘルパー・モック生成（`mockgen` / `testify`）
- CI 上の `go test ./...` 実行ステップ追加
- カバレッジレポートの可視化

**期待効果:** テストスコアが 1 → 5 点以上に改善

---

### 優先度3: セキュリティ設計・実装

**概要:** 認証・認可・入力バリデーションを設計段階から組み込む。

- JWT / セッション管理の方針策定
- GraphQL ディレクティブによる認可チェック
- 機密情報の環境変数管理（`.env.example` 作成）
- `golangci-lint` + `gosec` による静的解析を CI に追加

**期待効果:** セキュリティスコアが 1 → 5 点以上に改善

---

## 改善 Issue の起票

> すべての観点でスコアが 5 点未満のため、以下の改善 Issue を起票します。

| # | タイトル | 優先度 | 観点 |
|---|---------|--------|------|
| TBD | [改善] backend 基盤実装：ディレクトリ構成・Go モジュール・共有ユーティリティ | High | アーキテクチャ・コード品質 |
| TBD | [改善] backend テスト基盤整備：単体テスト・CI 統合・カバレッジ計測 | High | テスト |
| TBD | [改善] backend セキュリティ設計：認証・認可・静的解析（gosec）導入 | Medium | セキュリティ |
| TBD | [改善] backend パフォーマンス設計：N+1 対策・クエリ最適化・キャッシュ戦略 | Medium | パフォーマンス |
| TBD | [改善] backend 運用性強化：ビルド・デプロイパイプライン・ロギング・ヘルスチェック実装 | Medium | 運用性 |
